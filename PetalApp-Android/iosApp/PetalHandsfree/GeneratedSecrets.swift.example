// Example only — the real GeneratedSecrets.swift is generated at build time
// by ../Scripts/generate_secrets.sh from the GEMINI_API_KEY environment
// variable (mirrors Android's local.properties -> BuildConfig.GEMINI_API_KEY).
// This file is never compiled; it just documents the generated shape.
enum GeneratedSecrets {
    static let geminiAPIKey = "ADD_YOUR_KEY_HERE"
}
